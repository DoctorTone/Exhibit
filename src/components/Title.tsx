import Typography from "@mui/material/Typography";

const Title = () => {
  return (
    <div id="title" className="panel">
      <Typography variant="h3" sx={{ fontStyle: "italic", color: "#555" }}>
        Pterodactylus
      </Typography>
    </div>
  );
};

export default Title;
